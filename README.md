# Quản lí kho hàng
Dự án quản lí lưu trữ hàng hóa 

# cách sử dụng github
1. Đẩy file github
kiểm tra: `git status`
thêm file: `git add .`
. là lấy hết
nhắn tin: `git commit -m "tin nhắn"`
đẩy lên `git push <tên repo> <nhánh>`
ví dụ: 
```
git push origin main
git push origin alpha
```
